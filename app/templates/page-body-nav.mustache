<div class="page-body-nav">
    <div class="navContent">
        {{#nav}}
            <div class="navLeft">
                <div class="itemBriefly">
                    <ul>
                        {{#each items}}
                            <li index="{{@index}}">
                                <h3 class="title">{{title}}</h3>
                                {{#each content}}<a class="content" href="{{.}}">{{@key}}</a>{{/each}}
                                <span class="fa fa-angle-right fa-fw"></span>
                            </li>
                        {{/each}}
                    </ul>
                </div>
            </div>
            <div class="navRight">
                <div class="showContainer">
                    <div class="show_banner">
                        {{#each bannerImg}}
                            {{#each .}}
                                <a href="{{.}}" class="bannerImg">
                                    <img src="{{@key}}"/>
                                </a>
                            {{/each}}
                        {{/each}}
                        <span class="carousel_list">
                            {{#each bannerImg}}
                                <i class="carousel_i {{#isFirst @index "showed"}}{{/isFirst}}"
                                   index="{{@index}}"></i>
                            {{/each}}
                        </span>
                        <a class="carousel prev"><</a>
                        <a class="carousel next">></a>
                    </div>
                    <div class="show_info">
                        {{#infoImg}}
                            {{#each .}}
                                <a href="{{.}}"><img src="{{@key}}"/></a>
                            {{/each}}
                        {{/infoImg}}
                    </div>
                </div>
                <div class="itemDetails" style="display:none">
                    {{#each items}}
                        <div index="{{@index}}" style="display:none">
                            <ul class="classify">
                                {{#more.classify}}
                                    <li>
                                        <span class="title">{{title}}</span>
                                        <div class="content">
                                            {{#each content}}
                                                <a href="{{.}}">{{@key}}</a>
                                            {{/each}}
                                        </div>
                                    </li>
                                {{/more.classify}}
                            </ul>
                            <ul class="recommend">
                                {{#more.recommend}}
                                    <li>
                                        {{#each .}}
                                            <a href="{{.}}"><img src="{{@key}}"/></a>
                                        {{/each}}
                                    </li>
                                {{/more.recommend}}
                            </ul>
                        </div>
                    {{/each}}
                </div>
            </div>
        {{/nav}}
    </div>

</div>